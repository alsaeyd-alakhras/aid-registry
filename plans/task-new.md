هذا البرومبت النهائي المعدل بشكل واضح ومباشر تعطيه لـ Cursor:

---

# Task: شاشة عرض جميع المساعدات (Datatable) مع بيانات الأسرة

المطلوب إنشاء صفحة:

```
resources/views/aid_distributions/index.blade.php
```

مرتبطة بـ:

```
AidDistributionController
```

---

## 🔹 المرجع

روح على:

* مجلد `offices`
* مجلد `executives`

وخد منهم:

* index.blade.php
* دوال controller التالية:

  * index
  * getFilterOptions
  * destroy

واستخدمهم كأساس للبناء (نفس أسلوب Datatable + نفس هيكل البحث + نفس طريقة الفلترة).

---

## 🔹 المطلوب في هذه الصفحة

عرض **كل عمليات المساعدة** في Datatable
لكن مع عرض بيانات الأسرة داخل نفس الجدول.

يعني الصف الواحد يمثل عملية صرف واحدة، ويتضمن:

### بيانات المساعدة:

* distributed_at
* office
* aid_mode
* cash_amount أو aid_item
* created_by

### بيانات الأسرة (من العلاقة):

* primary_name
* national_id
* mobile
* family_members_count
* housing_location

---

## 🔹 Controller – index()

في دالة `index()`:

* جلب كل المساعدات مع العلاقات:

  ```
  AidDistribution::with(['family', 'office', 'aidItem', 'creator'])
  ```

* استخدام `map()` بعد الجلب
  لتحويل البيانات إلى structure مناسب للـ Datatable

يعني:

* تاخد بيانات المساعدة
* ومن `$distribution->family` تجيب بيانات الأسرة
* وترجعهم في array واحد للعرض

---

## 🔹 فلترة السنوات (مثل executives)

روح على صفحة `executives`
حتلاقي فلتر years فوق الجدول.

انسخ نفس الفكرة هنا:

* دالة getFilterOptions()
* dropdown للسنوات
* فلترة حسب سنة `distributed_at`

---

## 🔹 تعديل سلوك زر Edit

في صفحات executives أو offices
حتلاقي عند الضغط على edit بفتح modal.

❌ هذا غير مطلوب هنا.

بدل ذلك:

* زر Edit يروح على:

  ```
  route('aid_distributions.edit', $id)
  ```

يعني يفتح صفحة التعديل الكاملة، مش modal.

---

## 🔹 destroy()

نفس أسلوب destroy الموجود في الصفحات المرجعية
لكن يشتغل على `aid_distributions`.

---

## 🔹 ملاحظات مهمة

* لا تغير تنسيق Datatable.
* لا تعيد بناء UI.
* فقط عدل الأعمدة والبيانات المعروضة.
* أهم شيء عرض:

  * تاريخ المساعدة
  * بيانات الأسرة الأساسية كاملة
* لا تستخدم modal في edit.
* اربط الأزرار بالـ routes الخاصة بـ AidDistributionController.

---

الهدف:

صفحة عرض احترافية تعرض كل عمليات الصرف،
مع دمج بيانات الأسرة داخل نفس الجدول،
بنفس أسلوب النظام الحالي 100% بدون كسر الـ structure.
