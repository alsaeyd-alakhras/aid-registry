ملاحظاتك ممتازة لأنها تجعل النظام **مرن للموظف لكن بدون فقدان التحكم**. سأعيد صياغة القواعد بحيث تصبح **مواصفات تنفيذ فعلية** وليست مجرد أفكار.

سأعيد كتابة القواعد بعد التعديل بحيث تكون جاهزة للتطبيق.

---

# ✅ قواعد النظام بعد التعديل (النسخة التنفيذية)

سنركز فقط على القواعد المتأثرة بملاحظاتك.

---

## 🟢 القاعدة 1 — البحث عن الأسرة قبل الإدخال (بدون منع)

### الهدف

تجنب إدخال الأسرة مرة ثانية، لكن **بدون منع الإدخال**.

---

### طريقة العمل

بجانب حقل رقم الهوية يوجد:

```
[ رقم الهوية ] [ زر بحث ]
```

عند الضغط على زر البحث:

النظام يبحث في:

```
families.national_id
families.spouse_national_id
```

---

### إذا لم توجد بيانات

يكمل الموظف الإدخال طبيعي.

---

### إذا وجدت بيانات

يظهر **Pop جانبي** يحتوي:

### القسم 1 — بيانات الأسرة

- الاسم
- رقم الهوية
- الجوال
- مكان السكن
- عدد أفراد الأسرة

مع زر:

```
[ نسخ بيانات الأسرة ]
```

عند الضغط:

- تعبأ الحقول الحالية في الفورم.
- لا يتم حفظ شيء في قاعدة البيانات.
- فقط تعبئة الحقول.

---

### القسم 2 — سجل المساعدات السابقة

يعرض:

آخر **10 مساعدات فقط**:

لكل مساعدة:

- تاريخ
- مكتب الصرف
- نوع المساعدة
- قيمة أو صنف

وبجانب كل عنصر:

```
[ 👁 عرض ]
```

يفتح Pop ثاني يعرض تفاصيل المساعدة كاملة.

---

### القسم 3 — سجل الزوج/الزوجة

إذا كان الزوج/الزوجة مسجلين في أسرة أخرى:

يظهر:

```
تم العثور على سجل باسم الزوج/الزوجة
```

مع زر:

```
عرض المساعدات
```

ويعرض مساعداتهم.

---

### القرار النهائي

✔ يسمح بإضافة مساعدة جديدة دائمًا.

✔ فقط يعطي تحذير ومعلومات سابقة.

---

## 🟢 القاعدة 2 — إضافة مساعدة جديدة للأسرة الموجودة

إذا الأسرة موجودة:

النظام:

- لا ينشئ أسرة جديدة.
- يستخدم نفس family_id.
- يضيف فقط سجل مساعدة جديد.

---

## 🟢 القاعدة 3 — منع تكرار الأسرة عند الحفظ

إذا المستخدم أدخل بيانات أسرة موجودة:

بدل إنشاء سجل جديد:

```
update families
```

ثم:

```
insert aid_distributions
```

أي:

- تحديث بيانات الأسرة
- إضافة عملية صرف فقط

---

## 🟢 القاعدة 4 — نسخ البيانات بدون إدخال جديد

زر:

```
نسخ بيانات الأسرة
```

يقوم بـ:

- تعبئة الحقول فقط.
- لا يحفظ شيء.
- عند الحفظ يتم تحديث الأسرة.

---

## 🟢 القاعدة 5 — تحذير الزوج/الزوجة

إذا:

هوية الزوج/الزوجة لها سجل سابق:

يعرض:

```
تم تسجيل الزوج/الزوجة مسبقاً واستلم مساعدات.
```

مع عرض مساعداتهم.

لكن لا يمنع الصرف.

---

## 🟢 القاعدة 6 — تحقق نوع المساعدة

إذا:

```
aid_mode = cash
```

يجب إدخال مبلغ.

إذا:

```
aid_mode = in_kind
```

يجب اختيار صنف.

---

## 🟢 القاعدة 7 — عدم حذف عمليات الصرف

بدل الحذف:

```
status = cancelled
```

---

# ✅ التقييم المهني لملاحظاتك

قراراتك الحالية تحقق:

✔ إدخال سريع

✔ منع تكرار بدون تعقيد

✔ عدم تعطيل الموظفين

✔ مرونة للمؤسسة

✔ بيانات نظيفة للتقارير

وأهم شيء:

النظام لن يكره المستخدم.

---